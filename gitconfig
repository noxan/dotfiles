[color]
    status = auto
    diff = auto
    merge = auto
    log = auto
    commit = auto
[alias]
    st = status
    co = checkout
    ci = commit
    br = branch
    lg = "log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
[user]
    name = noxan
    email = noxan@redmoonstudios.de
